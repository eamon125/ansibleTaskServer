[copy]
alias=文件复制
config=playbook/copy.yml

[cron]
alias=定时任务
config=playbook/cron.yml

[fetch]
alias=远程拉取文件
config=playbook/fetch.yml

[file]
alias=文件操作
config=playbook/file.yml

[hostname]
alias=主机名管理
config=playbook/hostname.yml

[ignore]
alias=忽略错误
config=playbook/ignore.yml

[lineinfile]
alias=文本操作
config=playbook/lineinfile.yml

[mutual]
alias=互信操作
config=playbook/mutual.yml

[owner]
alias=指定文件用户和分组
config=playbook/owner.yml

[ping]
alias=ping 主机
config=playbook/ping.yml

[script]
alias=本地脚本在远程主机运行
config=playbook/script.yml

[setup]
alias=系统信息
config=playbook/setup.yml

[shell]
alias=shell操作
config=playbook/shell.yml

[template]
alias=模板管理
config=playbook/template.yml

[timezone]
alias=时区管理
config=playbook/timezone.yml

[unarchive]
alias=文件解压
config=playbook/unarchive.yml

[user]
alias=用户管理
config=playbook/user.yml

[when]
alias=if条件过滤
config=playbook/when.yml

[withitem]
alias=循环操作
config=playbook/withitem.yml